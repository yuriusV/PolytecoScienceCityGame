
/**
 * @returns {{name: string, money: number, respect: number}[]}
 */
const getRecordInformation = () => {};

/**
 * 
 * @param {{name: string, money: number, respect: number}} record 
 */
const pushOwnRecord = (record) => {};

export default {
	getRecordInformation,
	pushOwnRecord
};